# Development Environment Overrides
# This file contains development-specific settings that override .env defaults
# Used in docker-compose.yml env_file configuration

# Force DEBUG=True for development static file serving
DEBUG=True

# Enable Django static file serving in development
DJANGO_SERVE_STATIC=True

# Development logging
LOG_LEVEL=DEBUG
